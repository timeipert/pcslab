import { NoteDragDirective } from './note-drag.directive';

describe('NoteDragDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteDragDirective();
    expect(directive).toBeTruthy();
  });
});
